// TODO: Write a js program to find maximum and minimum between two numbers using functions.

const minmax = (num1, num2) => {
  let min = Math.min(num1, num2);
  let max = Math.max(num1, num2);

  console.log(`min: ${min}, max: ${max}`);
};

minmax(7668, 98764667686);
