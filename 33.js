// TODO: Write a js program to find maximum between two numbers using conditional operator.

const maxNum = (num1, num2) => {
  num1 > num2 ? console.log(num1) : console.log(num2);
};

maxNum(324324, 32);
